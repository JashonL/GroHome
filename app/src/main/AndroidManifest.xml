<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.growatt.grohome">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--位置权限-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 这个权限用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" /> <!-- 访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="TUYA_SMART_APPKEY"
            android:value="fheuyt49ajggpqqffy7f" />
        <meta-data
            android:name="TUYA_SMART_SECRET"
            android:value="p5exhqm5maetnxgjrfry3h7xvgy74grj" />

        <activity android:name=".module.login.RegisterLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

        </activity>

        <activity android:name=".module.home.RoomListActivity"/>

        <activity android:name=".module.device.DeviceTypeActivity"/>

        <activity android:name=".module.config.WiFiOptionsActivity"/>

        <activity android:name=".module.config.DeviceLightStatusActivity"/>

        <activity android:name=".module.config.DeviceConfigActivity"/>

        <activity android:name=".module.config.ConnectHotsPotActivity"/>

        <activity android:name=".module.config.DeviceEZLightActivity"/>

        <activity android:name=".module.config.DeviceAPLightActivity"/>

        <activity android:name=".module.config.WiFiSetGuideActivity"/>

        <activity android:name=".module.config.SelectConfigTypeActivity"/>

    </application>

</manifest>